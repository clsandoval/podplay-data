# Pod Play Distribution

Master franchise distribution of Pod Play and Ping Pod across Southeast Asia. Operates the country-license + per-store fee model; not the same as `podplay/` (which is the on-the-ground SEA install business) or `helios-pickleball-center` (one specific flagship venue venture).

**Status:** Stub — populated as engagements with country licensees and store-level franchisees materialize.

## Likely structure (when content lands)

- `knowledge/franchise-model.md` — country/per-store fee economics, JDC reference, Robinsons Land relationship
- `data/licensees/` — country/region master licensees (Thailand, Singapore, etc.)
- `data/stores/` — per-store franchisees under each licensee
- `data/contracts/` — franchise agreements
- `data/leads/` — countries / partners in conversation but not yet signed

## Cross-references

- Relates to `[[pod-play-sea]]` (the operating venture in monorepo terms)
- See `cs/monorepo/entities/projects/ping-pod-asia-franchise.md` for current franchise model context: $100K country fee, $28K per-store fee
